import { Grid, Paper } from "@mui/material";
import React from "react";

const SelectorJobsArea = () => {
    return (
        <Paper>
            <Grid container padding={2} marginTop={1}></Grid>
        </Paper>
    );
};

export default SelectorJobsArea;
