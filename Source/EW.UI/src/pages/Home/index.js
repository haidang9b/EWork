import { Container } from "@mui/system";
import React from "react";
import SearchBar from "../../components/SearchBar";

const Home = () => {
    return (
        <Container>
            <SearchBar />
        </Container>
    );
};

export default Home;
