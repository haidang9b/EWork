/**
 * Get page name with application name
 * @param {string} page is name of page, ex: Home
 * @returns {string} string name of page - name of app, ex: Home - EWork
 */
export const getPageName = (page) => {
    return `${page} - EWork`;
};
